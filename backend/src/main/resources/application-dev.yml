spring:
  jpa:
    show-sql: true
    hibernate:
      # Dev: allow additive column updates (e.g. coupons.min_order_amount)
      ddl-auto: update

logging:
  level:
    com.aetherpass: DEBUG
    org.hibernate.SQL: WARN
